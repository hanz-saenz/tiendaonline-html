{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Django: Tienda",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/manage.py",
            "args": [
                "runserver",
                "--noreload"
            ],
            "django": true,
            "python": "${workspaceFolder}/../venv_tienda/Scripts/python",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "console": "internalConsole"
        
        }
    ]
}